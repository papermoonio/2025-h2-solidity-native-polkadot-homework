# MintableERC20 合约代码详解

## 📋 合约概述

`MintableERC20` 是一个基于 OpenZeppelin ERC20 标准的可铸造代币合约，增加了**时间限制的铸造功能**和**所有者权限管理**。

---

## 🏗️ 合约结构

### 继承关系
- **继承自**: `ERC20` (OpenZeppelin)
- **功能**: 标准 ERC20 代币功能 + 自定义铸造逻辑

---

## 📦 状态变量

```solidity
mapping(address => uint) public lastMintTime;  // 记录每个地址最后一次铸造的时间戳
uint256 public interval;                       // 两次铸造之间需要等待的时间间隔（秒）
address public owner;                          // 合约所有者地址
```

### 变量详解

| 变量 | 类型 | 说明 |
|------|------|------|
| `lastMintTime` | `mapping(address => uint)` | 映射表，记录每个用户最后一次铸造的时间戳 |
| `interval` | `uint256` | 默认 **3600 秒**（1小时），限制用户铸造频率 |
| `owner` | `address` | 合约部署者，拥有特殊权限（铸造、设置间隔） |

---

## 🔧 构造函数 (Constructor)

```solidity
constructor(string memory _name, string memory _symbol)
```

### 功能说明

1. **初始化 ERC20**: 设置代币名称和符号
2. **自动铸造**: 部署时给部署者铸造 **100,000** 个代币
3. **设置 owner**: 将部署者设置为合约所有者
4. **设置间隔**: 将铸造间隔设置为 **3600 秒**（1小时）

### 初始化流程

```
部署合约
  ↓
设置代币名称和符号 (ERC20)
  ↓
给部署者铸造 100,000 个代币
  ↓
设置 owner = msg.sender
  ↓
设置 interval = 3600 秒
```

### 示例

```solidity
// 部署时
MintableERC20 token = new MintableERC20("MyToken", "MTK");

// 结果:
// - 代币名称: "MyToken"
// - 代币符号: "MTK"  
// - 部署者获得 100,000 MTK
// - interval = 3600 秒（1小时）
```

---

## 🎯 核心功能函数

### 1. `mintToken()` - 用户自助铸造

```solidity
function mintToken() public
```

**功能**: 允许任何用户为自己铸造代币，但有**时间限制**

**规则**:
- ✅ **首次铸造**: 用户首次调用时直接允许
- ⏰ **时间限制**: 距离上次铸造必须超过 `interval` 秒（默认1小时）
- 🪙 **铸造数量**: 每次固定铸造 **100** 个代币
- 📝 **记录时间**: 更新用户的 `lastMintTime`

**逻辑流程**:
```
检查条件:
  IF lastMintTime[msg.sender] == 0 
     → 允许（首次铸造）
  ELSE IF block.timestamp > lastMintTime[msg.sender] + interval
     → 允许（已过冷却期）
  ELSE
     → 拒绝，抛出错误: "You need to wait an hour between mints"

执行铸造:
  _mint(msg.sender, 100000000000000000000)  // 100 个代币
  lastMintTime[msg.sender] = block.timestamp  // 更新时间戳
```

**使用示例**:
```solidity
// 用户首次铸造
user.mintToken()  
// ✅ 成功，获得 100 个代币

// 立即再次铸造
user.mintToken()  
// ❌ 失败: "You need to wait an hour between mints"

// 等待 1 小时后
// ... 时间推进 3601 秒 ...
user.mintToken()  
// ✅ 成功，再次获得 100 个代币
```

**安全考虑**:
- 使用 `require` 检查时间限制
- 每个用户独立计时（`mapping` 结构）
- 防止用户频繁铸造导致通胀

---

### 2. `ownerMint()` - 所有者铸造

```solidity
function ownerMint(address _target, uint256 _amount) public onlyOwner
```

**功能**: 合约所有者可以为**任何地址**铸造**任意数量**的代币

**权限**: 仅 `owner` 可以调用（使用 `onlyOwner` 修饰符）

**参数**:
- `_target`: 接收代币的地址
- `_amount`: 铸造的代币数量（wei 单位）

**特点**:
- ⚡ **无时间限制**: Owner 可以随时铸造
- 🎯 **指定地址**: 可以为任何地址铸造
- 💰 **任意数量**: 可以指定任意铸造数量
- 🔒 **权限控制**: 只有 owner 可以调用

**使用示例**:
```solidity
// Owner 操作
owner.ownerMint(user1, 1000 * 10^18)  // 给 user1 铸造 1000 个代币
owner.ownerMint(user2, 5000 * 10^18)  // 给 user2 铸造 5000 个代币
owner.ownerMint(contract, 10000 * 10^18)  // 给合约地址铸造
```

**安全警告**:
- Owner 权限很大，需要妥善保管私钥
- 建议在生产环境使用多签钱包或时间锁

---

### 3. `canMint()` - 查询是否可以铸造

```solidity
function canMint(address _address) public view returns (bool)
```

**功能**: 查询指定地址是否可以**立即**铸造代币

**返回值**:
- `true`: 可以铸造（从未铸造过 **或** 已过冷却时间）
- `false`: 不能铸造（还在冷却时间内）

**判断逻辑**:
```solidity
return 
  lastMintTime[_address] == 0  OR  // 从未铸造过
  block.timestamp > lastMintTime[_address] + interval  // 已过冷却期
```

**使用场景**:
- 前端界面显示"可以铸造"或"冷却中"
- 智能合约中提前判断是否可以铸造
- 用户体验优化

**使用示例**:
```solidity
// 查询用户是否可以铸造
if (token.canMint(user1)) {
    // 显示"可以铸造"按钮
    token.mintToken();
} else {
    // 显示"冷却中，请等待 X 秒"
}
```

---

### 4. `setInterval()` - 设置铸造间隔

```solidity
function setInterval(uint _newInterval) public onlyOwner
```

**功能**: 允许 owner 修改铸造间隔时间

**权限**: 仅 `owner` 可以调用

**参数**:
- `_newInterval`: 新的间隔时间（秒）

**使用场景**:
- 根据业务需求调整铸造频率
- 可以设置为更短（如 30 分钟）或更长（如 2 小时）
- 灵活性管理代币铸造规则

**使用示例**:
```solidity
// 修改为 30 分钟间隔
owner.setInterval(1800)  

// 修改为 2 小时间隔  
owner.setInterval(7200)  

// 修改为 1 天间隔
owner.setInterval(86400)
```

**注意事项**:
- 修改后立即生效
- 不影响之前的 `lastMintTime` 记录
- 建议设置合理的时间间隔

---

## 🔒 访问控制

### `onlyOwner` 修饰符

```solidity
modifier onlyOwner() {
    require(msg.sender == owner, "Not owner");
    _;
}
```

**功能**: 确保只有合约所有者可以调用特定函数

**受保护函数**:
- `ownerMint()`: 只有 owner 可以铸造给他人
- `setInterval()`: 只有 owner 可以修改间隔

**安全检查**:
- 使用 `require` 验证调用者身份
- 如果非 owner 调用，会抛出 `"Not owner"` 错误并回滚交易

**使用示例**:
```solidity
// Owner 调用 ✅
owner.ownerMint(user, 1000)  // 成功

// 普通用户调用 ❌
user.ownerMint(user2, 1000)  // 失败: "Not owner"
```

---

## 📊 代币数量说明

### 初始化铸造（部署时）
- **数量**: **100,000** 个代币
- **接收者**: 合约部署者
- **时间**: 部署时自动执行
- **计算**: `100000000000000000000000` wei = 100,000 tokens

### 用户铸造 (`mintToken()`)
- **数量**: **100** 个代币（固定）
- **频率**: 每 `interval` 秒一次（默认 3600 秒 = 1 小时）
- **接收者**: 调用者自己
- **计算**: `100000000000000000000` wei = 100 tokens

### 所有者铸造 (`ownerMint()`)
- **数量**: **任意**（由 owner 指定）
- **频率**: **无限制**
- **接收者**: 任意地址（由 owner 指定）

---

## 🔄 时间机制详解

### 时间戳记录
- 使用 `block.timestamp` 记录铸造时间
- Solidity 中 `block.timestamp` 返回**区块时间戳**（秒级精度）
- 存储在 `lastMintTime` 映射中

### 冷却时间计算
```
可铸造条件:
  lastMintTime == 0  OR  block.timestamp > lastMintTime + interval
```

**示例**:
```
用户 A:
  - 首次调用 mintToken() 
    → lastMintTime[A] = 0 → 允许铸造
    → 铸造后: lastMintTime[A] = 1000 (假设当前时间戳)
  
  - 5 秒后再次调用
    → block.timestamp = 1005
    → lastMintTime[A] + interval = 1000 + 3600 = 4600
    → 1005 < 4600 → 拒绝铸造
  
  - 1 小时后再次调用
    → block.timestamp = 4601
    → 4601 > 4600 → 允许铸造
    → 更新 lastMintTime[A] = 4601
```

### 时间单位
- 所有时间以**秒**为单位
- `interval` 默认值为 `3600`（1小时）
- `block.timestamp` 是区块时间戳（秒级精度，最小单位：秒）

---

## ⚠️ 重要注意事项

### 1. 时间精度与测试
- Solidity 的 `block.timestamp` 精度为秒
- 在测试中可以使用 Hardhat 的 `evm_increaseTime` 模拟时间推移
- 区块时间戳由矿工设置，可能有小幅度偏差

### 2. 安全性考虑
- **Owner 权限**: Owner 权限很大，可以无限铸造任意数量
  - 建议在生产环境使用**多重签名钱包**
  - 或者添加**时间锁**限制 owner 操作
- **时间操控**: 虽然 `block.timestamp` 可能被矿工操控，但影响很小（±15秒）
- **用户隔离**: `lastMintTime` 对每个地址单独跟踪，互不影响

### 3. 用户体验
- 用户首次铸造无限制（`lastMintTime == 0`）
- 后续铸造需要等待 `interval` 时间
- 可以通过 `canMint()` 提前检查，改善用户体验

### 4. 可扩展性
- 继承自 OpenZeppelin ERC20，支持标准代币操作
- 可以与其他 DeFi 协议兼容
- 支持转账、授权、转账等标准 ERC20 功能

### 5. Gas 优化
- 使用 `mapping` 存储用户时间戳，查找效率 O(1)
- `require` 在条件不满足时立即回滚，节省 gas
- `view` 函数 `canMint()` 不消耗 gas（本地调用）

---

## 💡 使用场景示例

### 场景 1: 游戏代币系统
```solidity
// 玩家每天可以免费领取一次代币
// 设置间隔为 86400 秒（1天）
owner.setInterval(86400);

// 玩家领取
player.mintToken();  // 获得 100 个代币
```

### 场景 2: 奖励系统
```solidity
// 用户完成任务后可以每小时领取奖励
// 默认间隔 3600 秒（1小时）
user.completeTask();
user.mintToken();  // 领取奖励
```

### 场景 3: 社区代币分发
```solidity
// Owner 为社区成员空投
owner.ownerMint(communityMember1, 10000 * 10^18);
owner.ownerMint(communityMember2, 5000 * 10^18);
```

---

## 🔍 代码流程图

### mintToken() 执行流程
```
开始
  ↓
检查 lastMintTime[msg.sender]
  ↓
是 0?  → [是] → 允许铸造
  ↓ [否]
block.timestamp > lastMintTime + interval?
  ↓
[是] → 允许铸造
[否] → 抛出错误 "You need to wait an hour between mints"
  ↓
_mint(msg.sender, 100 tokens)
  ↓
更新 lastMintTime[msg.sender] = block.timestamp
  ↓
结束
```

### ownerMint() 执行流程
```
开始
  ↓
检查 msg.sender == owner
  ↓
[是] → 允许
[否] → 抛出错误 "Not owner"
  ↓
_mint(_target, _amount)
  ↓
结束
```

---

## 📝 总结

`MintableERC20` 合约是一个功能完善的代币合约，主要特点：

✅ **标准化**: 继承 OpenZeppelin ERC20，兼容所有标准协议  
✅ **限时铸造**: 防止用户频繁铸造，控制通胀  
✅ **权限管理**: Owner 拥有灵活的管理权限  
✅ **用户友好**: 提供 `canMint()` 查询函数  
✅ **可配置**: Owner 可以调整铸造间隔  

适合用于：游戏代币、奖励系统、社区代币分发等场景。

--- 
启动测试链
npx hardhat node --port 8545

power expand audit jelly rocket pattern dignity cradle away climb high away