Create a new project (ethers.js + typescript)
```
npm init -y
npm install viem
npm install --save-dev typescript
npm install --save-dev ts-node @types/node
npx tsc --init
```

Run a typescript file
```
ts-node main.ts
```